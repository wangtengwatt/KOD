## 上传当前进度到 GitLab `suanbao_KOD`

1. **提交前复核**
   - 确认当前分支仍为 `suanbao_KOD`，上游为 `origin/suanbao_KOD`。
   - 查看最终状态和 diff 概览，确保没有在检查期间出现额外变更。

2. **验证当前代码**
   - 运行新增及修改过的蒜宝相关 Vitest 测试。
   - 运行 TypeScript 类型检查；若全库已有无关失败，将如实区分并报告，不会隐瞒或擅自修复其他模块。

3. **纳入全部当前改动**
   - 按你的明确选择执行全量暂存，包括：
     - 蒜宝源码、共享类型及测试；
     - `.zcode/` 会话计划；
     - `HANDOFF.md`；
     - `docs/README.md` 删除；
     - 其余所有已修改和未跟踪文件。
   - 暂存后再次核对清单。

4. **提交**
   - 使用与仓库历史一致的 Conventional Commit 风格创建一个描述当前蒜宝进度的提交，例如 `feat(suanbao): implement phase-one assistant runtime`。
   - 不修改或合并已有提交。

5. **推送并确认**
   - 将当前 `suanbao_KOD` 分支推送至 GitLab `origin/suanbao_KOD`。
   - 推送后核对本地 HEAD、上游跟踪状态及工作区状态，并报告提交哈希、测试结果和推送结果。