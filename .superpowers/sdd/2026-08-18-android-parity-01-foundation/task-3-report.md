# Task 3 report: canonical Android shell migration

Date: 2026-08-18

## Scope and outcome

Task 3 migrated the Android/mobile shell into the canonical repository without replacing the canonical compute, wallet, relay, authentication, or persistence implementations. The mobile shell now exposes the required five top-level destinations in this order: `对话`, `生图`, `视频`, `算力`, `我的`. The existing conversation routes retain exactly one canonical `RelayStationSelector` before their single `InputBox`.

The Android production identity is `com.kod.app`. Debug/local-test APKs use Gradle's `.localtest` application ID suffix and therefore install as `com.kod.app.localtest`. Production Capacitor configuration keeps HTTPS and cleartext/mixed content disabled; setting `KOD_ANDROID_LOCAL_TEST=1` enables the explicit local-test transport mode without changing the application ID source.

## Changed files

- `src/renderer/components/mobile/MobileBottomNavigation.tsx`: canonical mobile-only five-item bottom navigation, active-route state, exact Simplified Chinese labels, and bottom safe-area padding.
- `src/renderer/components/mobile/MobileBottomNavigation.test.tsx`: focused navigation contract test for labels, order, and routes.
- `src/renderer/routes/-mobile-route-contract.test.tsx`: focused route-tree contract plus AST checks that the existing home and session composers each contain one relay/node selector before one input.
- `src/renderer/routes/mobile-my.tsx`: minimal mobile account/settings surface using the canonical auth/settings stores and existing login modal; includes login/account switch and confirmed logout.
- `src/renderer/routes/__root.tsx`: mounts the mobile-only bottom navigation and reserves safe-area-aware content space on mobile. Desktop/Windows layout remains unchanged.
- `capacitor.config.ts`: production KOD identity and HTTPS defaults, plus opt-in local-test transport settings.
- `android/app/build.gradle`: production namespace/application ID/version and debug-only local-test identity/label.
- `android/app/src/main/java/com/kod/app/MainActivity.java`: canonical Java package for the Capacitor bridge activity (moved from `com/kai/kod`).
- `android/app/src/main/res/values/strings.xml`: KOD application label, package metadata, and `kod` custom URL scheme.

`src/renderer/routeTree.gen.ts` and Capacitor assets were regenerated by the normal tools and remain ignored/generated outputs rather than hand-maintained changes.

## Source-to-target mapping

| Read-only source reference | Canonical target and treatment |
| --- | --- |
| `D:/watt/kod-android/src/renderer/components/mobile/MobileBottomNavigation.tsx` | Adapted into the new canonical navigation component, but routes were bound to the canonical route tree and no source business logic was copied. |
| `D:/watt/kod-android/src/renderer/routes/mobile-my.tsx` | Reduced to a minimal canonical account/settings page. It uses `useAuthInfoStore`, `useSettingsStore`, `useAuthTokens`, and the current `settings/provider/chatbox-ai` login modal instead of the source repository's stale `kod-ai` business implementation. |
| `D:/watt/kod-android/src/renderer/setup/mobile_safe_area.ts` | Compared with the canonical file. The canonical implementation already matched the required native safe-area behavior, so it was retained unchanged. |
| `D:/watt/kod-android/android/app/build.gradle` and Android resources/`MainActivity.java` | Identity, debug suffix, label, namespace, and native startup package were migrated while retaining the canonical generated Capacitor project structure and SDK settings. |
| Canonical `src/renderer/routes/index.tsx` and `src/renderer/routes/session/$sessionId.tsx` | Preserved. Their shared `RelayStationSelector` and `InputBox` integration already met the required single-selector contract. |
| Canonical language defaults/persistence | Preserved. The canonical default and persisted migration already select `zh-Hans`; the new navigation makes the five required labels exact under that language. |

## TDD evidence

### RED

The two focused contract files were added before production changes and run with Node 22 plus the approved test-only `node-userinfo-enomem-shim.cjs` preload:

```text
C:\Program Files\nodejs\node.exe node_modules\vitest\vitest.mjs run \
  src/renderer/routes/-mobile-route-contract.test.tsx \
  src/renderer/components/mobile/MobileBottomNavigation.test.tsx
```

Result: 2 files failed, 2 tests failed, and 2 tests passed. The expected failures were:

- `the mobile bottom navigation module must exist: expected null not to be null`
- the route contract did not contain `/mobile-my`

Both existing selector/input assertions passed in RED, proving that this migration should preserve rather than duplicate that canonical composer UI.

### GREEN

The same focused run after implementation completed successfully:

```text
Test Files  2 passed (2)
Tests       4 passed (4)
```

The final navigation test explicitly asserts `对话`, `生图`, `视频`, `算力`, `我的` in product order and verifies their canonical destinations.

## Verification and build evidence

- Changed-file Biome check: passed (`Checked 6 files ... No fixes applied`).
- `pnpm run check` under Node 22 with the approved test/check-only shim: passed (`tsc --noEmit`, exit 0).
- Full Vitest suite under Node 22 with the approved test-only shim: passed, `139 passed | 2 skipped` files and `1321 passed | 54 skipped` tests.
- `git diff --check`: passed.
- Repository-wide `pnpm run lint`: attempted; the existing baseline reports 12 errors and 999 warnings in unrelated files. The changed-file Biome check is clean.

Production mobile synchronization was run unshimmed in the full host environment:

```text
C:\Program Files\nodejs\node.exe \
  C:\Users\Microsoft\AppData\Local\nodejs\node_modules\corepack\dist\pnpm.js \
  run mobile:sync:android
```

Result: passed. The Electron/Vite main, preload, and renderer production builds completed; sourcemaps were removed; Capacitor copied the web assets/config and synchronized all 12 Android plugins. The generated production Capacitor config contains `appId: com.kod.app`, `androidScheme: https`, `cleartext: false`, and `allowMixedContent: false`.

The Android debug APK was then built offline using the installed SDK and Gradle cache:

```text
android\gradlew.bat :app:assembleDebug --offline
```

Result: `BUILD SUCCESSFUL` (274 tasks). AAPT inspection of `android/app/build/outputs/apk/debug/app-debug.apk` reported package `com.kod.app.localtest`, version `0.1.0-localtest`, and label `KOD 本地测试`.

## Production endpoint audit and concerns

- No `10.0.2.2` literal is present in the synchronized production output.
- No KOD-owned production endpoint or generated Capacitor configuration points at `localhost`.
- The bundled third-party/runtime JavaScript still contains generic `localhost` literals used by URL parsing, router fallback origins, local-host detection, file URL validation, Sentry Spotlight defaults, and SSRF rejection logic. Removing or rewriting those dependency guard strings would alter vendor semantics and is outside Task 3. A Task 5 artifact policy should distinguish configured/reachable product endpoints from inert dependency validation/fallback strings if it performs a raw literal scan.
- The sandboxed unshimmed sync initially failed while loading `electron-vite` because Node's `os.userInfo()` returned `uv_os_get_passwd ENOMEM`. The exact same unshimmed sync succeeded with the full host token, confirming a sandbox-host issue. The shim was never used for product build, sync, or Gradle.
- Native debug build retains the canonical minimum SDK 23 and target SDK 35; this task did not broaden scope to alter the canonical SDK compatibility policy.

## Commit

Commit message: `feat(android): migrate mobile shell into canonical source`

Commit hash: this report is included in the Task 3 commit itself, so its immutable self-hash cannot be embedded without changing that hash. Resolve it as `git rev-parse HEAD`; the exact resulting hash is recorded in the controller handoff immediately after commit creation.
