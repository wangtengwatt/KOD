## 1. 建立基线与隔离测试设施

- [ ] 1.1 记录重构前 `pnpm check`、`pnpm check:biome` 和现有算力中心测试结果，区分既有失败与本变更回归
- [ ] 1.2 建立算力中心测试 fixture 和模拟 transport，保证未模拟网络请求会立即失败且 fixture 不包含真实账户或凭证
- [ ] 1.3 为未登录、普通购买方、已认证供应方、算力管理员和叠加角色补充页面特征测试，冻结现有导航、标签、入口与公开浏览行为
- [ ] 1.4 为现有 API 路径、HTTP 方法、请求字段、响应解析、业务错误码和公开导出补充契约测试
- [ ] 1.5 为现有 React Query 查询键、缓存共享和写成功后的失效范围补充特征测试
- [ ] 1.6 建立显式启用的真实环境只读冒烟脚本与路径白名单，只允许验证登录状态、商品列表和账户资产读取

## 2. 拆分 API 并保留兼容门面

- [ ] 2.1 创建 `src/renderer/packages/compute-center/` 目录并把公共类型迁移到 `types.ts`，验证类型导出不变
- [ ] 2.2 把基础 URL、鉴权、超时、响应解析和错误规范化迁移到共享 `client.ts`，覆盖 30 秒普通读取和 120 秒材料操作终止测试
- [ ] 2.3 把查询键迁移到 `queryKeys.ts`，保持现有键形状和缓存语义
- [ ] 2.4 按 account、marketplace、cardHours、packages、reservations、supplier、notifications、admin 拆分领域 API 并逐模块通过契约测试
- [ ] 2.5 将 `src/renderer/packages/computeCenter.ts` 改为兼容重导出门面，并验证旧入口和新入口的符号、类型与行为一致
- [ ] 2.6 将算力中心内部导入逐步切换到新领域入口，确认仓库中没有领域模块反向依赖兼容门面

## 3. 建立共享可靠性与安全能力

- [ ] 3.1 先为同一 action key 快速重复提交编写失败测试，再实现 `useComputeActionController` 的单操作防重和终态清理
- [ ] 3.2 为成功、业务失败、认证失败、网络失败、超时、取消和组件卸载补充测试，确保按钮不会永久加载且卸载后不更新状态
- [ ] 3.3 实现结果未知状态：写响应丢失时不自动重试，显示核对提示并只读刷新相关资产或记录
- [ ] 3.4 在现有接口支持请求标识的操作中复用同一用户意图的标识，并测试确认重试不会生成新标识
- [ ] 3.5 抽取卡时消耗控制器并测试余额充足、精确补足确认、取消零写入、补足后只续跑一次及双余额不足跳官网流程
- [ ] 3.6 实现 validation、business、auth、transport、unknown 错误归类和简体中文反馈映射，保留原服务端业务错误码
- [ ] 3.7 实现统一敏感字段脱敏函数，并用 API Key、令牌、身份证号、证件 URL、SSH 凭证、上游密钥和数据库凭证回归测试验证无泄露
- [ ] 3.8 实现完整 Token 套餐 API Key 的按需获取、内存短驻留、复制门禁以及隐藏、切页、退出和卸载清理测试

## 4. 拆分低风险页面领域

- [ ] 4.1 创建 route shell、领域目录、共享页面组件和纯工具目录，暂时通过适配层渲染原实现并保持界面不变
- [ ] 4.2 迁移实时行情与算力市场领域，验证商品搜索、分类、公开浏览、五秒行情刷新和原商品卡片行为不变
- [ ] 4.3 迁移通知领域，验证未读数量、通知列表、已读操作和三秒居中成功/失败反馈行为不变
- [ ] 4.4 迁移购买记录领域，验证 Token 套餐交付信息、模型、Base URL、API Key 显示与复制入口不变
- [ ] 4.5 为市场、行情、通知和购买记录分别安装领域 Error Boundary，并测试单个领域崩溃不影响顶栏、导航和其他领域

## 5. 拆分资产与订单领域

- [ ] 5.1 迁移我的资产领域，验证人民币余额、可用/冻结卡时、累计/租金/佣金收益、设备状态和常用入口不变
- [ ] 5.2 迁移我的订单领域，验证 GPU 订单状态、买卖双方信息、交付、争议、确认和历史记录展示不变
- [ ] 5.3 迁移 GPU/Token 购买与预订交互，把同一操作防重、卡时补足和结果未知处理接入现有入口
- [ ] 5.4 用模拟后端验证卡时足额购买、人民币补足后购买、取消补足、余额均不足、写响应丢失和快速双击场景均无重复扣款或重复下单
- [ ] 5.5 为资产、购买和订单分别安装领域 Error Boundary，并验证异常隔离和只读恢复入口

## 6. 拆分供应方与管理员领域

- [ ] 6.1 迁移供应方实名认证、设备与验机、产品发布、出租订单、交付和收益视图，保持现有状态、字段、权限与提示不变
- [ ] 6.2 迁移管理员审核、设备与订单、运营设置视图，验证管理员入口只由现有账户标志驱动
- [ ] 6.3 补充权限测试，保证供应方仍同时拥有购买方能力，非管理员既不显示也不请求管理员数据
- [ ] 6.4 为实名认证材料、设备证明、SSH 信息和管理员错误上下文补充安全回归测试，确认日志、通知和测试产物不含完整敏感值
- [ ] 6.5 为供应方与管理员分别安装领域 Error Boundary，并验证任一区域异常不会导致整个算力中心崩溃

## 7. 拆分卡时业务并收敛路由

- [ ] 7.1 将 `CardHourBusiness.tsx` 的卡时资产、现货/询价、转让、回购、提现、邀请奖励和记录视图按现有领域边界拆分
- [ ] 7.2 为卡时转让、回购、提现等资金相关写操作接入 action key 防重、结果未知与只读核对逻辑
- [ ] 7.3 通过特征测试验证兑换比例、到账去向、邀请奖励延迟、提现审核和现有卡时文案没有被重构改变
- [ ] 7.4 将 `compute-center.tsx` 收敛为身份上下文、顶栏、一级导航和领域容错编排，移除已迁移的内联业务实现
- [ ] 7.5 检查领域间依赖、循环导入和文件职责，使 feature 不读取其他 feature 私有实现且路由/API 文件达到设计中的维护性目标

## 8. 关键缺陷门禁与范围控制

- [ ] 8.1 针对登录状态、扣款、下单、敏感数据、页面崩溃和永久加载执行定向审计，并记录可复现证据
- [ ] 8.2 对审计发现的每个关键缺陷先增加失败测试，再实施不改变其他业务规则的最小修复
- [ ] 8.3 将不属于关键缺陷的发现逐项记录为独立 OpenSpec 变更候选，不在本重构中顺手改变行为
- [ ] 8.4 审核本变更差异，确认没有后端、数据库、官网钱包、零售站/节点、对话、生图、生视频或新运行时依赖变更

## 9. 四端验证与交付

- [ ] 9.1 运行全部算力中心 API、hook、组件、安全测试以及项目全量 `pnpm test`
- [ ] 9.2 运行 `pnpm check`、`pnpm check:biome` 和 `pnpm build`，完成 Windows 桌面版关键页面人工冒烟
- [ ] 9.3 运行 `pnpm build:web` 并验证 Web 端公开浏览、登录后资产、购买确认、订单和复制/外链能力
- [ ] 9.4 运行 `pnpm mobile:sync:android`，在本机 Android 工具链可用时执行 Gradle 编译并记录结果
- [ ] 9.5 运行 `pnpm mobile:sync:ios` 验证 iOS 共享 renderer 和 Capacitor 同步，并在 macOS/Xcode 或 macOS CI 完成原生编译与真机验收
- [ ] 9.6 使用真实环境只读冒烟脚本验证登录状态、商品列表和资产读取，确认脚本没有发出任何业务写请求
- [ ] 9.7 形成交付报告，列出已验证平台、未执行原生步骤、关键缺陷修复、独立 OpenSpec 候选和可回滚提交点
