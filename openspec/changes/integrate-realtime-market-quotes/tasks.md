## 1. Preserve and Audit Integration State

- [x] 1.1 Verify that `b8c6643` is an ancestor of the local branch, `47daf9c` remains layered above it, the worktree is clean, and `backup/pre-realtime-quotes-20260824` resolves to the protected combined state.
- [x] 1.2 Review the colleague commit and final range diff for duplicate cherry-picks, unresolved conflict markers, logo changes, unrelated settings changes, generated artifacts, and added secrets.

## 2. Verify the Colleague Market Feature

- [x] 2.1 Run the focused market-intelligence panel, adapter, contracts, simulation-feed, feature-flag, and variable test suites and record exact results.
- [x] 2.2 Verify the default shared endpoint, HTTPS origin allowlist, omitted credentials, remote-required error behavior, explicit simulation disclosure, and disabled write behavior against the specification.
- [x] 2.3 Run the compute-center route regressions needed to prove the local commit layered above the colleague change still works.

## 3. Repair Verified Defects

- [x] 3.1 For each defect found in Tasks 2.1-2.3, add and run a minimal failing regression test before editing production code; if the repair conflicts with protected local work or exceeds the specification, pause for user approval.
- [x] 3.2 Implement only the minimal repair on top of the colleague design and rerun all affected focused suites.

## 4. Validate the Client

- [x] 4.1 Run TypeScript checking and Biome on changed/colleague market files, classifying only demonstrably pre-existing baseline failures.
- [x] 4.2 Run the relevant broader regression suite and production build without changing branding or unrelated behavior.
- [x] 4.3 Validate `integrate-realtime-market-quotes` with OpenSpec strict validation and run Git whitespace, conflict-marker, protected-logo, unrelated-scope, and added-secret checks.

## 5. Synchronize Desktop Manual Testing

- [x] 5.1 Update the stable local launcher chain so `KOD蒜粒-开发版.lnk` starts the verified client from `D:\watt\kod`, while leaving `KAI - 快捷方式.lnk` and the KAI SVG untouched.
- [x] 5.2 Inspect the final shortcut target, arguments, working directory, delegated script, and log path, then perform a bounded startup smoke test and record the result.

## 6. Deliver Recovery and Verification Evidence

- [x] 6.1 Record commit ancestry, backup branch, tests, build, endpoint availability, OpenSpec validation, protected-scope scan, and launcher target in `verification.md`.
- [x] 6.2 Review the final diff with the user-approved scope and report any unexecuted manual or external-endpoint checks without claiming them as passed.

## 7. Address Independent Review Findings

- [x] 7.1 Correct the launcher design to match the inspected full-stack shortcut chain and explicitly leave the stale, unreferenced legacy script out of scope.
- [x] 7.2 Add a mutation-verified route regression proving the default compute-center tab uses the shared simulation adapter and exposes required-feed failure.
- [x] 7.3 Record exact verification commands and baseline references, then rerun focused tests, type checking, build, strict OpenSpec validation, and scope checks.
